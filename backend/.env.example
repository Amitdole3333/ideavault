# IdeaVault Backend — Environment Variables
# Copy this to .env and fill in your values

# Server
PORT=3001
NODE_ENV=development

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Database (PostgreSQL — e.g. Neon)
# Use pooler host (-pooler) for DATABASE_URL, direct host for DIRECT_URL (migrations).
# DATABASE_URL="postgresql://USER:PASSWORD@HOST-POOLER/DB?sslmode=require"
# DIRECT_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
DATABASE_URL="postgresql://user:password@host-pooler/dbname?sslmode=require"
DIRECT_URL="postgresql://user:password@host/dbname?sslmode=require"

# Algorand (Testnet)
ALGORAND_NETWORK=testnet
ALGORAND_NODE_URL=https://testnet-api.algonode.cloud
ALGORAND_INDEXER_URL=https://testnet-idx.algonode.cloud
ALGORAND_APP_ID=0
# Authentication: Use EITHER mnemonic OR private key (not both)
# Option 1: 25-word mnemonic (recommended for easy backup)
ALGORAND_DEPLOYER_MNEMONIC=your twenty five word mnemonic phrase goes here replace this with real values yes
# Option 2: Base64-encoded private key (alternative to mnemonic)
# ALGORAND_DEPLOYER_PRIVATE_KEY=base64_encoded_private_key_here
# Note: Mnemonic of the backend "service" account that pays for on-chain txns
# (Founder signs their own txn if wallet is connected; this is for backend-initiated calls)

# IPFS / Pinata (use JWT or API key + secret)
PINATA_API_KEY=your_pinata_api_key
PINATA_SECRET_API_KEY=your_pinata_secret_key
PINATA_JWT=
# Optional: JWT from Pinata dashboard (preferred for server). If set, API key/secret can be omitted.
PINATA_GATEWAY=https://gateway.pinata.cloud

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
